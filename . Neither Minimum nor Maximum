class Solution(object):
    def findNonMinOrMax(self, nums):
        """
        :type nums: List[int]
        :rtype: int
        """
        mn = min(nums)
        mx = max(nums)

        for num in nums:
            if num != mn and num != mx:
                return num

        return -1
